<h1>My Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %></li>
    <li><%= task.details %></li>
    <li><%= task.completed == true ? "Completed" : "Incomplete" %></li>
  <% end %>
  <%= link_to "Add a new Task", new_task_path %>
</ul>
