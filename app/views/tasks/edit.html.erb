<h1>Edit Tasks</h1>
<%= simple_form_for(@task) do |t| %>
  <%= t.input :title %>
  <%= t.input :details %>
  <%= t.input :completed %>
  <%= t.submit %>
<% end %>

<p><%= link_to "Back to list", tasks_path %></p>
